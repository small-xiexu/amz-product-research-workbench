# 全链路可用性提升计划

## 目标

让任意 Agent 打开本项目后，能从 Stage 1 自然走到 Stage 13 完成产出，不依赖人工救火。

## 当前状态

| 阶段 | 状态 | 说明 |
|------|------|------|
| Stage 1-4 | 已跑通 | `run_pipeline.py` + Quick Agent spawn，16 个 issue 已修复 |
| Stage 5-8 | 已修复待验证 | 脚本/prompt/契约已对齐，未在真实 run 中端到端验证 |
| Stage 9-13 | 正在 debug | 用户在边跑边记录问题 |

## 完成标准

新上下文打开项目 → 读 SKILL.md → 按 dispatch 规则 spawn Agent → 每阶段末尾按脚本清单执行 → 产出完整报告 + QA 审查通过。

## 待办

1. [x] Stage 1-4 契约补齐 + 流程编排
2. [x] Stage 5-8 问题修复（#9-#16）
3. [x] Stage 9-13 debug 并修复发现的问题（#20 Lead Operator 重写、#21 Report Agent 削权 已完成）
4. [x] 在 SKILL.md 每个 Stage 末尾补齐脚本调用清单（见下方）
5. [ ] 用新品类从头跑到 Stage 13，验证全链路闭环
6. [ ] 扩展 `run_pipeline.py` 至全链路编排（可选）

## SKILL.md 各 Stage 脚本清单（待写入）

### Stage 1-4（已有 run_pipeline.py）
- `init_workflow_state.py` → Quick Agents spawn → `fill_quick_packet_contract.py`×2 → `build_quick_market_gate.py` → `build_mcp_candidate_pool.py`

### Stage 5 路线矩阵
- `build_route_matrix_confirm.py [--force-confirm]`

### Stage 6 深挖
- Market Structure Agent + Search Demand Agent 并行 spawn
- Agent 产出后：`build_deep_snapshot.py`×2 → `build_deep_evidence_packet.py`×2

### Stage 7 冲突复核
- `build_conflict_review.py`

### Stage 8 VOC
- `build_review_asin_batch.py` → 用户导出评论 → `build_review_voc_package.py` → `build_voc_gate.py`

### Stage 9 六维评价
- 6 个 Evaluation Agent 推荐并行 spawn → 输出到 `evaluations/`

### Stage 10 资深判断
- Lead Operator Agent spawn → `evaluations/lead_operator_judgment.json`

### Stage 11 Seed
- `seed_report_data.py`（从 evaluations/ 生成 report_data.seed.json）

### Stage 12 报告
- Report Generation Agent spawn（读 seed → 写 report_data.json + HTML）

### Stage 13 QA
- `xlsx_back_table.py` → `delivery_qa.py` → Delivery QA Agent 强制 spawn

## 同步更新项
- `README.md` 常用入口表
- `multi_agent_dispatch.md` Stage 依赖链
- `SKILL.md` 各 Stage 末尾脚本清单
