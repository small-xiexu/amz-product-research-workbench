# V1 选品系统实施计划

更新日期：2026-06-08

这份文件是项目唯一进度台账。下次恢复任务时，先读这里，再看代码和其他文档。

## 当前状态

| 项 | 状态 |
|---|---|
| 当前阶段 | V1 骨架 |
| 当前重点 | P11 准备下一轮报告质量优化和规则收敛 |
| 远程同步点 | 已推送到远程 `main`，下次恢复以 `git log -1 --oneline` 核对 |
| 是否已接 API/MCP | 未接 |
| 下一步是否需要 API Key | 不需要；P7 先做手动导出/导入闭环，API/MCP 等后续预算允许再增强 |

## 方向锚点

- 入口不是具体产品，而是模糊选品意图。
- 初始阶段不问采购价、FBA、头程、入库配置费。
- 系统先发现候选品池，再对重点候选做深挖报告。
- 数据接入优先按卖家精灵手动导出/导入设计；API/MCP 作为后续自动化增强。
- 正式深挖必须有 Top100 完整明细、基础属性打标、交叉分析和数据质量检查。
- 利润只做参考，关键字段由运营在利润复核阶段补。
- 评论插件只在重点候选深挖阶段接入。

## 已完成

- [x] 建立 GitHub 项目和基础目录
  - 验收：`main` 已推送到 `small-xiexu/amz-product-research-workbench`
- [x] 整理项目方向锚点
  - 验收：`docs/选品系统方向锚点.md`
- [x] 更新 V1 范围、流程方案、技术方案、字段来源表
  - 验收：文档已统一为“选品意图 -> 候选品池 -> 重点候选深挖”
- [x] 搭建 Skill 骨架
  - 验收：`skills/seller-sprite-product-research/SKILL.md`
- [x] 搭建本地报告生成器骨架
  - 验收：`scripts/build_mock_report.py` 可从样例 `research_package` 生成报告、摘要、HTML 看板和 Excel
- [x] 吸收运营 Q&A 成本口径
  - 验收：仓储费 3%、FBA 手填、入库配置费手填、头程计费重取大等规则已进入核心文档
- [x] 吸收开源 Skill 深挖质量标准
  - 验收：Top100 完整、属性打标、交叉分析、变体口径、结论回表已进入核心文档
- [x] 建立本计划台账
  - 验收：本文件存在，并在 README 中可找到

## 下一步

- [x] P1 定义 `selection_brief` 数据结构
  - 状态：已完成
  - 产物：`packages/research_core/schemas/selection_brief.schema.json`
  - 样例：`examples/minimal_selection_brief.json`
  - 验收：能表达站点、禁区、大概方向、偏好、供应链优势、运营假设
  - 验收依据：已新增 schema 和最小样例，并通过 `python3 -m json.tool` 格式验证

- [x] P2 定义 `candidate_pool` 数据结构
  - 状态：已完成
  - 产物：`packages/research_core/schemas/candidate_pool.schema.json`
  - 样例：`examples/minimal_candidate_pool.json`
  - 验收：每个候选方向包含出现原因、需求证据、竞争结构、新品机会、风险、缺失数据、状态
  - 验收依据：已新增 schema 和最小样例，并通过 `python3 -m json.tool` 格式验证

- [x] P3 打通本地 mock 链路
  - 状态：已完成
  - 产物：`scripts/build_candidate_pool_mock.py`
  - 验收：`selection_brief -> candidate_pool` 可本地跑通，不依赖 API/MCP
  - 验收依据：`python3 scripts/build_candidate_pool_mock.py examples/minimal_selection_brief.json /tmp/research_candidate_pool_mock/candidate_pool.json` 已生成候选品池，并通过 `python3 -m json.tool` 格式验证

- [x] P4 从候选品池生成重点候选 `research_package`
  - 状态：已完成
  - 产物：转换脚本或规则函数
  - 验收：可从一个候选方向生成现有报告生成器可消费的数据包
  - 验收依据：`selection_brief -> candidate_pool -> research_package -> report/summary/dashboard/data.xlsx` 本地链路已跑通

- [x] P5 补利润规则实现口径
  - 状态：已完成
  - 产物：利润规则函数更新
  - 验收：仓储费默认按建议售价 3%，入库配置费不确定时待补，不默认 0，头程计费重取实际重和体积重较大值
  - 验收依据：已更新 `packages/research_core/rules/profit_rules.py`，并用本地样例验证仓储费 3%、计费重取大、入库配置费缺失时报错

- [x] P6 准备卖家精灵 API/MCP 字段映射
  - 状态：已完成；已确认官方开放 API 文档和 Java demo，优先按 HTTP API 设计
  - 触发条件：P1-P4 本地链路跑通后再做
  - 验收：明确哪些候选发现字段来自哪个 API endpoint / MCP 工具、哪些需要手填、哪些需要人工复核
  - 验收依据：已更新技术方案、字段来源表、方向锚点、V1 范围、路线图、Skill 映射和数据契约；已通过 JSON 格式校验、脚本编译和本地 mock 报告链路验证

- [x] P7 接入卖家精灵手动导出/导入数据，API/MCP 后续增强
  - 状态：已完成；真实卖家精灵导出数据已可生成候选品池初版
  - 触发条件：需要真实候选发现时
  - 验收：能读取卖家精灵手动导出的 Excel/CSV 或整理表，用真实数据生成候选品池
  - 已完成：客服已确认 API/MCP 需额外付费，V1 主线调整为 AI 指挥用户手动导出卖家精灵数据并导入分析
  - 已完成：已收到 `卖家精灵导出样例_美国站_宠物牵引绳_20260607`，当前最全样例包含搜索结果表、关键词反查表、ABA 关键词表和卖家精灵市场分析 Excel
  - 已完成：市场分析 Excel 可结构化读取市场概况、行业需求/销售趋势、商品/品牌/卖家集中度、卖家类型/所属地、A+视频、退货率、上架/评分/价格分布
  - 已完成：已新增 `import_manifest` schema、样例 manifest、`inspect_manual_exports.py` 盘点脚本和 `requirements.txt`
  - 已完成：用当前样例生成 manifest，已自动识别搜索结果、市场分析、关键词反查和 ABA 关键词表，缺失来源为 0
  - 已完成：已新增 `build_candidate_pool_from_import_manifest.py`，可从真实导出 manifest 生成 `candidate_pool`
  - 验收依据：当前宠物牵引绳样例已跑通 `manual exports -> import_manifest -> candidate_pool -> research_package -> report/summary/dashboard/data.xlsx`
  - 下一步：进入重点候选深挖前，先确认是否使用当前 `Hands Free Leashes` 作为评论/VOC 试点

- [x] P7.5 生成候选品池预审输出
  - 状态：已完成；已补充 `candidate_pool -> precheck report/summary/dashboard/data.xlsx` 输出链路
  - 触发条件：P7 已从真实导出数据生成候选品池后
  - 验收：候选池能生成可读预审报告、摘要、HTML 看板和 Excel 底表，用于判断哪些候选进入重点深挖
  - 验收依据：当前宠物牵引绳样例已生成 `precheck_report.md`、`precheck_summary.md`、`precheck_dashboard.html` 和 `precheck_data.xlsx`，Excel 可被 `openpyxl` 读取
  - 下一步：确认是否使用当前 `Hands Free Leashes` 进入评论/VOC 深挖

- [x] P8 接入自有评论插件
  - 状态：已完成；采用文件导入方式接入插件 Excel，HTML AI 报告作为辅助参考
  - 触发条件：重点候选进入正式深挖后
  - 验收：读取评论 Excel，HTML AI 报告只作阅读参考，痛点结论可追溯到评论
  - 已完成：确认插件是浏览器扩展，已稳定导出 `评论数据` Excel 和 HTML AI 报告
  - 已完成：新增 `review_voc_package` schema 和 `build_review_voc_from_plugin_export.py`
  - 验收依据：已用 `B07R56CBWX-multi-2026-06-07.xlsx/html` 生成 `review_voc_package.json`、`voc_report.md`、`voc_summary.md`、`voc_evidence.xlsx`；Excel 可被 `openpyxl` 读取；另用 `B088NRLMPV-2026-06-07.xlsx/html` 完成单 ASIN 验证
  - 下一步：把 `review_voc_package` 合并到重点候选 `research_package` 和最终报告

- [x] P9 合并评论 VOC 到重点候选深挖报告
  - 状态：已完成；`research_package` 已可引用 `review_voc_package`，并在主报告/Excel 中展示评论痛点和证据
  - 触发条件：P8 已生成 `review_voc_package`
  - 验收：`research_package` 能引用评论来源、写入痛点/亮点/改品机会，并在主报告和 Excel 中保留评论 ID 追溯
  - 验收依据：已跑通 `manual exports -> candidate_pool -> review_voc_package -> research_package -> report/summary/dashboard/data.xlsx`；最终 Excel 新增 `评论VOC` 和 `VOC证据` Sheet，证据行包含评论 ID、ASIN、评分、日期、片段和链接；旧的不带 VOC 命令仍可运行
  - 下一步：优化市场字段映射，让报告里的市场规模、价格带、竞品池不再显示过多待填

- [x] P10 优化深挖报告的市场字段映射和竞品池分组
  - 状态：已完成；已从真实候选池补市场规模、价格带、Top10 和近半年新品分组
  - 触发条件：P9 已完成
  - 验收：深挖报告能从真实候选池写入市场规模、价格带、Top10/近半年新品/结构补充组，并减少不必要的待填字段
  - 验收依据：当前样例已生成市场规模、价格带、品牌集中度、卖家结构、新品机会；竞品池输出 Top10 标杆组 10 个、近半年放量新品组 4 个、结构补充组 4 个；主报告和 Excel `竞品池` Sheet 已可查看
  - 下一步：围绕报告质量继续收敛判断规则、利润待补提醒和状态卡

- [ ] P11 准备下一轮报告质量优化和规则收敛
  - 状态：待开始；P10 后报告已更完整，但状态卡、利润待补、知产/合规提示仍偏骨架
  - 触发条件：P10 已完成
  - 验收：报告能更清楚地区分事实、推断、待补和建议，并减少运营阅读时的误解成本

## 下次恢复顺序

1. 读取本文件。
2. 找到第一个未完成的 `P` 项。
3. 开始前把该项状态改成“进行中”。
4. 完成并验证后再勾选。
5. 不要跳过 `selection_brief` 和 `candidate_pool` 直接接手动导入/API/MCP。

## 何时需要你提供卖家精灵导出数据 / API Key / MCP

当前已经完成 P1-P6。P7 先做手动导出/导入闭环，暂不需要 API Key 或 MCP。  

需要数据时，我会明确告诉你：

- 需要从卖家精灵哪个功能页面导出什么表
- 要跑哪个站点
- 要用什么关键词/大概方向测试
- 需要哪些列、文件命名和筛选条件
- 后续如果切 API/MCP，再说明需要哪个 endpoint、`secret-key` 权限或 MCP 工具
