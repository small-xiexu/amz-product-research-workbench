# V1 选品系统实施计划

更新日期：2026-06-13

这份文件是项目唯一进度台账。下次恢复任务时，先读这里，再看代码和其他文档。

## 当前状态

| 项 | 状态 |
|---|---|
| 当前阶段 | V1 骨架 |
| 当前重点 | P24 已完成；下一步待定 |
| 远程同步点 | P23 已推送到远程 `main`；P24 本地改动待提交/推送 |
| 是否已接 Sorftime MCP | 已接（2026-06-11）；`sorftime-server` 已添加到用户级 MCP 配置，连接状态 ✔ Connected |
| 三源融合策略 | 卖家精灵手动导入 = 广域候选池；Sorftime MCP = 趋势/竞品/关键词深度验证；评论插件 = VOC 证据 |
| 架构原则（2026-06-11确认） | 脚本 = 纯数据工具（读取/清洗/结构化/可视化）；Claude = 真正的分析者，在对话中提供 VOC 解读、竞品判断、选品建议 |

## 方向锚点

- 入口不是具体产品，而是模糊选品意图。
- 初始阶段不问采购价、FBA、头程、入库配置费。
- 系统支持两种入口：无方向探索先导入卖家精灵 `选市场 200 条` + Sorftime `search_categories_broadly` 扫类目；指定方向深挖再由 AI 给 2-3 个精准关键词并引导后续导出，Sorftime 补关键词趋势和竞品流量词验证。
- 系统先发现候选品池，再对重点候选做深挖报告。
- 数据接入优先按卖家精灵手动导出/导入设计；Sorftime MCP 作为深度验证层在 V1 同步接入，覆盖类目趋势、关键词趋势、竞品流量词、相似品和潜力品；评论 VOC 仍由自有评论插件承担。
- 正式深挖必须有 Top100 完整明细、基础属性打标、交叉分析和数据质量检查。
- 利润只做参考，关键字段由运营在利润复核阶段补。
- 评论插件只在重点候选深挖阶段接入，且输入以候选池筛出的 ASIN 批次为准，不是零散单品。
- 报告必须基于真实导入数据给中肯建议，事实、推断、风险和下一步要分清；视觉和看板只服务判断，不做花哨但无实际作用的展示。

## 待统一优化

- [已落地/P18] 关键词阶段固定为“AI 先给 2-3 个主关键词 + 1 个备选词”，避免一次性给大词包导致卖家精灵导出混池。
- [已落地/P18] 无方向探索模式固定为“卖家精灵选市场 200 条 + 运营边界”，第一轮不要求关键词，先筛候选市场/类目方向。
- [已落地/P18] 第一轮导出前增加“初始候选假设卡”：AI 根据运营的模糊方向先梳理 2-4 个可能分支、混池风险、初始排除项和推荐主线，让运营选择更想深入了解的方向；如果运营不调整，则按 AI 推荐主线继续，并在报告里记录该假设。
- 候选发现阶段要先看大盘，再切细分路线：例如先看大类体量和退货/集中度，再拆产品路线、场景路线、价格路线，不能一开始只盯单个 ASIN。
- [已落地/P18] 卖家精灵导出阶段要做成明确操作引导：告诉用户先导出哪些市场/搜索报表，再基于搜索结果推荐若干 ASIN 去做关键词反查。
- ABA 数据选品可作为第一轮增强导出项：市场分析/搜索结果/关键词反查跑通后，优先用 2-3 个主关键词或重点 ASIN 查询最近完整月份的 ABA，用于校验真实搜索、点击和转化集中度。
- 关键词分析要做交叉验证：卖家精灵关键词、ABA、SIF、Google 趋势分别判断需求、点击/转化集中度、竞品占词和趋势，报告里要区分需求词、转化词、竞品占领词、趋势词和混池词。
- [已落地/P17/P18] 候选池阶段要输出“下一步建议抓评论的 ASIN 批次”，让用户直接复制到自有评论插件，而不是自己猜。
- [已落地/P18] 候选池预审后、评论 VOC 前必须增加“候选边界二次校准”：把混池场景、目标产品形态和排除规则给运营确认，再按确认后的边界重筛 ASIN 批次，不能直接用泛词 Top ASIN 抓评论。
- 增加人工产品形态标签层：AI 先按标题、类目、卖点打产品路线/场景/功能标签，运营可以修正；报告按修正后的标签做分布和交叉分析。
- [已落地/P18] 竞品明细要区分“展示行”和“唯一商品”：同一 ASIN 可能因广告位、自然位、关键词或变体重复出现，集中度、Top10 和商品数量必须以明确口径计算。
- 增加重点竞品深拆卡：对标杆老品、新品、争议竞品分别沉淀卖点、痛点、价格/促销、广告/站外、流量词、可借鉴点和不可复制点。
- 未满足需求不能只写痛点，要转成验证动作：供应商问询项、样品测试项、Listing 风险提示、A+ 图片/视频要解释的点。
- [部分已落地/P11/P18] 最终报告要强化结论链路：事实、运营判断、风险、建议、待补项、下一步动作分开写，让用户不用翻多个 Sheet 才知道为什么继续看或放弃。
- 利润模板拆成简版和详版：简版用于早期快速筛选，详版参考运营自研表，支持体积重/实际重取大、不同站点/物流、VAT、广告费率、退款率和两层毛利。
- 增加 Q&A / Listing 风险提示模块：把评论痛点和运营经验转成购买前提醒、适配边界、禁用场景和 FAQ，减少误购与差评。
- [已落地/P18] 每次用户放入导出文件后，必须先做数据盘点和缺失项检查，再进入候选池或报告生成。
- [已落地/P18] 导入外部 Excel 时要做有效区域裁剪和格式污染处理：例如 Sheet 实际只有十几列但 Excel 报告到 16384 列时，读取器不能把空格式列当成有效数据。
- 如果候选名称、类目、关键词、市场数据明显不一致，流程要暂停并提示人工确认，不能硬跑完整报告。
- 全球商标库暂不纳入 V1 主流程，避免早期流程过重；报告仅保留商标/命名风险待人工复核提示。
- 外观专利因单独收费，不纳入 V1 主流程；报告仅提示外观/结构专利待人工或专业工具复核。

## 已完成

- [x] 建立 GitHub 项目和基础目录
  - 验收：`main` 已推送到 `small-xiexu/amz-product-research-workbench`
- [x] 整理项目方向锚点
  - 验收：`docs/选品系统方向锚点.md`
- [x] 更新 V1 范围、流程方案、技术方案、字段来源表
  - 验收：文档已统一为“选品意图 -> 候选品池 -> 重点候选深挖”
- [x] 搭建 Skill 骨架
  - 验收：`skills/amazon-product-research/SKILL.md`
- [x] 搭建本地报告生成器骨架
  - 验收：`scripts/build_mock_report.py` 可从样例 `research_package` 生成报告、摘要、HTML 看板和 Excel
- [x] 吸收运营 Q&A 成本口径
  - 验收：仓储费 3%、FBA 手填、入库配置费手填、头程计费重取大等规则已进入核心文档
- [x] 吸收开源 Skill 深挖质量标准
  - 验收：Top100 完整、属性打标、交叉分析、变体口径、结论回表已进入核心文档
- [x] 建立本计划台账
  - 验收：本文件存在，并在 README 中可找到

## 下一步

- [x] P1 定义 `selection_brief` 数据结构
  - 状态：已完成
  - 产物：`packages/research_core/schemas/selection_brief.schema.json`
  - 样例：`examples/minimal_selection_brief.json`
  - 验收：能表达站点、禁区、大概方向、偏好、供应链优势、运营假设
  - 验收依据：已新增 schema 和最小样例，并通过 `python3 -m json.tool` 格式验证

- [x] P2 定义 `candidate_pool` 数据结构
  - 状态：已完成
  - 产物：`packages/research_core/schemas/candidate_pool.schema.json`
  - 样例：`examples/minimal_candidate_pool.json`
  - 验收：每个候选方向包含出现原因、需求证据、竞争结构、新品机会、风险、缺失数据、状态
  - 验收依据：已新增 schema 和最小样例，并通过 `python3 -m json.tool` 格式验证

- [x] P3 打通本地 mock 链路
  - 状态：已完成
  - 产物：`scripts/build_candidate_pool_mock.py`
  - 验收：`selection_brief -> candidate_pool` 可本地跑通，不依赖 MCP
  - 验收依据：`python3 scripts/build_candidate_pool_mock.py examples/minimal_selection_brief.json /tmp/research_candidate_pool_mock/candidate_pool.json` 已生成候选品池，并通过 `python3 -m json.tool` 格式验证

- [x] P4 从候选品池生成重点候选 `research_package`
  - 状态：已完成
  - 产物：转换脚本或规则函数
  - 验收：可从一个候选方向生成现有报告生成器可消费的数据包
  - 验收依据：`selection_brief -> candidate_pool -> research_package -> report/summary/dashboard/data.xlsx` 本地链路已跑通

- [x] P5 补利润规则实现口径
  - 状态：已完成
  - 产物：利润规则函数更新
  - 验收：仓储费默认按建议售价 3%，入库配置费不确定时待补，不默认 0，头程计费重取实际重和体积重较大值
  - 验收依据：已更新 `packages/research_core/rules/profit_rules.py`，并用本地样例验证仓储费 3%、计费重取大、入库配置费缺失时报错

- [x] P6 准备卖家精灵手动导出/MCP 字段映射
  - 状态：已完成；当前路线已调整为 V1 手动导出优先，后续只保留 MCP 自动化增强
  - 触发条件：P1-P4 本地链路跑通后再做
  - 验收：明确哪些候选发现字段来自卖家精灵手动导出、哪些后续可由 MCP 替换、哪些需要手填、哪些需要人工复核
  - 验收依据：已更新技术方案、字段来源表、方向锚点、V1 范围、路线图、Skill 映射和数据契约；已通过 JSON 格式校验、脚本编译和本地 mock 报告链路验证

- [x] P7 接入卖家精灵手动导出/导入数据，MCP 后续增强
  - 状态：已完成；真实卖家精灵导出数据已可生成候选品池初版
  - 触发条件：需要真实候选发现时
  - 验收：能读取卖家精灵手动导出的 Excel/CSV 或整理表，用真实数据生成候选品池
  - 已完成：客服已确认 HTTP API 和 MCP 分开收费，且 MCP 成本更低；项目路线调整为 V1 手动导出，后续只考虑 MCP，不再规划 HTTP API
  - 已完成：已收到 `卖家精灵导出样例_美国站_宠物牵引绳_20260607`，当前最全样例包含搜索结果表、关键词反查表、ABA 关键词表和卖家精灵市场分析 Excel
  - 已完成：市场分析 Excel 可结构化读取市场概况、行业需求/销售趋势、商品/品牌/卖家集中度、卖家类型/所属地、A+视频、退货率、上架/评分/价格分布
  - 已完成：已新增 `import_manifest` schema、样例 manifest、`inspect_manual_exports.py` 盘点脚本和 `requirements.txt`
  - 已完成：用当前样例生成 manifest，已自动识别搜索结果、市场分析、关键词反查和 ABA 关键词表，缺失来源为 0
  - 已完成：已新增 `build_candidate_pool_from_import_manifest.py`，可从真实导出 manifest 生成 `candidate_pool`
  - 验收依据：当前宠物牵引绳样例已跑通 `manual exports -> import_manifest -> candidate_pool -> research_package -> report/summary/dashboard/data.xlsx`
  - 下一步：进入重点候选深挖前，先确认是否使用当前 `Hands Free Leashes` 作为评论/VOC 试点

- [x] P7.5 生成候选品池预审输出
  - 状态：已完成；已补充 `candidate_pool -> precheck report/summary/dashboard/data.xlsx` 输出链路
  - 触发条件：P7 已从真实导出数据生成候选品池后
  - 验收：候选池能生成可读预审报告、摘要、HTML 看板和 Excel 底表，用于判断哪些候选进入重点深挖
  - 验收依据：当前宠物牵引绳样例已生成 `precheck_report.md`、`precheck_summary.md`、`precheck_dashboard.html` 和 `precheck_data.xlsx`，Excel 可被 `openpyxl` 读取
  - 下一步：确认是否使用当前 `Hands Free Leashes` 进入评论/VOC 深挖

- [x] P8 接入自有评论插件
  - 状态：已完成；采用文件导入方式接入插件 Excel，HTML AI 报告作为辅助参考
  - 触发条件：重点候选进入正式深挖后
  - 验收：读取评论 Excel，HTML AI 报告只作阅读参考，痛点结论可追溯到评论
  - 已完成：确认插件是浏览器扩展，已稳定导出 `评论数据` Excel 和 HTML AI 报告
  - 已完成：新增 `review_voc_package` schema 和 `build_review_voc_from_plugin_export.py`
  - 验收依据：已用 `B07R56CBWX-multi-2026-06-07.xlsx/html` 生成 `review_voc_package.json`、`voc_report.md`、`voc_summary.md`、`voc_evidence.xlsx`；Excel 可被 `openpyxl` 读取；另用 `B088NRLMPV-2026-06-07.xlsx/html` 完成单 ASIN 验证
  - 下一步：把 `review_voc_package` 合并到重点候选 `research_package` 和最终报告

- [x] P9 合并评论 VOC 到重点候选深挖报告
  - 状态：已完成；`research_package` 已可引用 `review_voc_package`，并在主报告/Excel 中展示评论痛点和证据
  - 触发条件：P8 已生成 `review_voc_package`
  - 验收：`research_package` 能引用评论来源、写入痛点/亮点/改品机会，并在主报告和 Excel 中保留评论 ID 追溯
  - 验收依据：已跑通 `manual exports -> candidate_pool -> review_voc_package -> research_package -> report/summary/dashboard/data.xlsx`；最终 Excel 新增 `评论VOC` 和 `VOC证据` Sheet，证据行包含评论 ID、ASIN、评分、日期、片段和链接；旧的不带 VOC 命令仍可运行
  - 下一步：优化市场字段映射，让报告里的市场规模、价格带、竞品池不再显示过多待填

- [x] P10 优化深挖报告的市场字段映射和竞品池分组
  - 状态：已完成；已从真实候选池补市场规模、价格带、Top10 和近半年新品分组
  - 触发条件：P9 已完成
  - 验收：深挖报告能从真实候选池写入市场规模、价格带、Top10/近半年新品/结构补充组，并减少不必要的待填字段
  - 验收依据：当前样例已生成市场规模、价格带、品牌集中度、卖家结构、新品机会；竞品池输出 Top10 标杆组 10 个、近半年放量新品组 4 个、结构补充组 4 个；主报告和 Excel `竞品池` Sheet 已可查看
  - 下一步：围绕报告质量继续收敛判断规则、利润待补提醒和状态卡

- [x] P11 准备下一轮报告质量优化和规则收敛
  - 状态：已完成；已补事实/推断/待补/建议、风险矩阵和下一步动作清单
  - 触发条件：P10 已完成
  - 验收：报告能更清楚地区分事实、推断、待补和建议，并减少运营阅读时的误解成本
  - 验收依据：当前样例已生成带 `决策检查` 和 `风险矩阵` 的 `report.md`；Excel 已新增 `决策检查`、`风险矩阵` Sheet；已通过脚本编译、真实样例链路和 `git diff --check`
  - 下一步：把多条命令收敛成一个编排入口，减少手动操作成本

- [x] P12 一键编排完整流程
  - 状态：已完成；已新增编排脚本，把卖家精灵导出、可选评论插件导出和报告生成串起来
  - 触发条件：P11 已完成
  - 验收：输入卖家精灵导出文件夹和可选评论插件 Excel/HTML 后，一条命令生成候选池、VOC 包、深挖包、报告、摘要、HTML 看板和 Excel
  - 验收依据：`scripts/run_research_workflow.py` 已用当前样例跑通；输出 `import_manifest.json`、`candidate_pool.json`、`review_voc_package.json`、`research_package.json`、`final_report/*` 和 `workflow_summary.md`；README 已补一键命令
  - 下一步：进入利润复核模板和计算闭环

- [x] P13 利润复核模板和计算闭环
  - 状态：已完成；已补利润模板生成、读取、计算和报告合并
  - 触发条件：P12 已完成
  - 验收：能生成利润手填模板，读取建议售价、采购价、站点汇率、FBA费用、头程、入库配置费，并按仓储费 3%、广告费率 20%、类目退货率计算基础 FBA 毛利和扣广告/退货后的毛利率
  - 验收依据：`scripts/build_profit_template.py` 可生成 `profit_review_template.xlsx`；`scripts/apply_profit_review.py` 可读取填好模板并写回 `profit_reference`、`profit_review`、决策检查和风险矩阵；`scripts/run_research_workflow.py --profit-template` 已用当前样例跑通；最终报告和 Excel 已展示两层 FBA 毛利、毛利率和成本拆分
  - 下一步：进入知产/合规初筛输入和回填闭环

- [x] P14 知产/合规初筛输入和回填闭环
  - 状态：已完成；已把 `docs/知产与合规检索入口库.md` 转成初筛模板、人工回填和报告合并逻辑
  - 触发条件：P13 已完成
  - 验收：能根据候选方向生成知产/合规复核模板，读取商标/专利/认证初筛结果，并回填报告中的待补项、风险矩阵和下一步动作
  - 验收依据：`scripts/build_ip_compliance_template.py` 可生成 `ip_compliance_review_template.xlsx`；`scripts/apply_ip_compliance_review.py` 可读取填好模板并写回 `ip_screening`、`compliance_screening`、`ip_compliance_review`、决策检查和风险矩阵；`scripts/run_research_workflow.py --profit-template --ip-compliance-template` 已用当前样例跑通；最终报告和 Excel 已展示知产/合规总状态、整体风险、待复核项、证据摘要和展开明细
  - 下一步：进入数据质量检查、属性打标和交叉分析基础版

- [x] P15 数据质量检查、属性打标和交叉分析基础版
  - 状态：已完成；已接入 Top100 商品明细质量检查、基础属性打标、属性分布、属性交叉分析和伪空白提示
  - 触发条件：P14 已完成
  - 验收：能对 Top100 明细做基础数据质量检查和低风险属性打标，输出属性分布、低置信度待确认项、基础交叉分析和伪空白原因提示
  - 验收依据：已生成基础版与利润/合规回填版报告预览，路径为 `/private/tmp/research_workbench_p15_preview_full/final_report/report.md`
  - 下一步：把报告预览给运营评审，收集对数据质量、属性结构、利润和知产/合规展示的修改意见

- [ ] P16 报告预览交付与运营反馈回收
  - 状态：已提交并推送；当前网页版报告评审版已收敛，用户确认“这版差不多了”
  - 触发条件：P15 已完成
  - 验收：收回运营对报告结构、字段展示、风险表达和流程节奏的反馈，并据此微调下一版口径
  - 已完成：dashboard 首屏、市场、关键词、竞品/VOC、利润/风险五大块已重做；Excel 底表保持可填写；金额字段已改为全局口径说明优先；竞品散点图纵轴已压缩且点位补了数值；页脚不再显示数据源
  - 已完成：两个深色强调块已切到更浅的蓝灰系，准备重新预览确认是否与整体暖色 UI 更一致
  - 已验证：最新预览里这两块已经不再是深色块，和页面主体的暖色底更协调
  - 已完成：散点图改为只标注 6 个代表点，其余点靠 hover 和表格承接，明显降低了图面拥挤感
  - 已验证：最新预览已重新生成，标签数已收敛到代表点级别
  - 进行中：散点图仍缺少“这图到底在看什么”的读图指引，准备补四象限/参考线和更直白的结论注释
  - 已完成：散点图顶部新增结论条和四象限读图提示，当前样本被解释为“低价冲量 + 中价过渡 + 高价守盘”三层结构
  - 已完成：竞品左侧主视图已从大散点图替换为“价格 × 销量分层”四象限卡片，表格保留为明细兜底
  - 已验证：最新预览已显示低价冲量、高价高销、低价低销、高价低销四块卡片，堆叠问题明显缓解
  - 已提交：`cc8a70a feat(report): 收敛V1选品报告评审版`，并已推送到远程 `main`
  - 下一步：进入 P16.5，把当前类目的评论插件导出回填到最终报告，再进入 P17 多样例验收

- [x] P16.5 当前样例评论 VOC 回填
  - 状态：已完成；已收到 `评论插件导出_免手持牵引绳_20260608`
  - 触发条件：P16 报告评审版已收敛后，优先补当前样例评论证据
  - 验收：能读取评论插件导出的 Excel / HTML AI 报告，明确区分采集入口站点和评论/市场判断站点，并把差评痛点、好评卖点、改品机会、VOC 证据回填到最终报告和 Excel
  - 备注：用户说明因美国站只能看到 8 条评论，实际通过加拿大站入口采集到更完整评论；报告需记录为“采集入口 CA，评论样本主要来自美国站语境/美国评论内容”，不能误判为加拿大市场
  - 验收依据：已跑通 `卖家精灵导出样例_美国站_宠物牵引绳_20260607` + `评论插件导出_免手持牵引绳_20260608` 的完整流程；`review_voc_package.json`、`report.md`、`dashboard.html`、`data.xlsx` 已写入采集入口站点、评论地区分布和口径说明
  - 下一步：进入 P17 多样例验收和默认模板固化

- [ ] P17 多样例验收和默认模板固化
  - 状态：进行中
  - 触发条件：P16 当前评审版提交后
  - 验收：至少用第二组卖家精灵真实导出数据跑通完整流程，确认报告模板不是只适配宠物牵引绳样例
  - 进行中：已在「刮窗器 / Extendable Window Squeegee」样例上验证候选池生成，修正了候选名称串台和多文件读取口径，继续补多样例验收
  - 进行中：用户已补充 ABA 数据选品导出；已新增 `ABAKeywordTrend` 识别规则，当前刮窗器样例可识别 `amazon_aba_keywords` 数据源；全球商标库已确认不纳入 V1 主流程
  - 进行中：已生成刮窗器候选池预审，输出 ABA 关键词信号、混池风险词和建议评论 VOC ASIN 批次；预审 Excel 包含 `ABA关键词`、`建议VOC ASIN` Sheet
  - 运营反馈：贴膜方向偏小且鸡肋，当前目标应收窄为“刮窗器 + 海绵/布垫、刮条 + 海绵垫”等窗户清洁组合工具；汽车/贴膜/浴室不作为主线，只作为混池风险或旁支机会
  - 运营确认：主线按“刮条 + 海绵/布垫 + 窗户清洁组合工具”看，偏二合一/三合一；可伸缩长杆是加分项不是必须；短柄、浴室短柄可保留参考；汽车挡风玻璃为可附带场景；带桶、替换布垫、多刮条套装保留；单独清洁液/清洁剂排除；标题含 car/shower 但形态符合组合工具时保留
  - 已完成：已盘点 `评价导出_刮窗器_20260609`，其中 `B0BKTM56C3-multi-2026-06-09.xlsx` 含 16 个 ASIN、1153 条有效评论；采集入口为加拿大站，评论地区以 United States 为主
  - 已完成：已用刮窗器卖家精灵导出 + 评论导出跑通完整流程，输出 `/tmp/amz_window_squeegee_p17_review_workflow/final_report/dashboard.html`、`report.md`、`data.xlsx`
  - 已完成：多样例验收暴露并修正两类泛化问题：属性路线不再残留“基础免手持牵引”，VOC 规则新增清洁工具痛点识别；当前首要 VOC 痛点为“长杆/连接/锁扣不稳”
  - 已验证：脚本编译通过，`git diff --check` 通过，最终报告已接入评论 VOC、采集入口/评论地区口径和刮窗器属性路线
  - 下一步：把当前刮窗器 HTML/Excel 报告给用户/运营快速评估；若方向认可，再补利润模板、知产/合规模板，或继续修正报告展示细节

- [ ] P18 从 0 到 1 流程优化版
  - 状态：进行中；第一批流程骨架优化已完成并验证，继续保留 P18 用于后续优化批次
  - 触发条件：用户确认先把优化点分批落地，再从 0 到 1 重新跑一版
  - 验收：能从模糊选品意图生成初始候选假设卡、2-3 个精准主关键词、卖家精灵导出引导；导入真实文件后能先做数据盘点/缺失检查，区分展示行和唯一商品，并在候选池预审里给出候选边界二次校准和建议评论 VOC ASIN 批次
  - 范围：本轮先落地流程骨架和报告可用性，不一次性吃完全部 20 个优化项
  - 已完成：曾新增 `scripts/build_zero_to_one_plan.py` 作为早期骨架；P24 已由 `interactive_workflow.py` 承接 Stage 0-1 并删除该脚本
  - 已完成：文档已明确两种探索模式：无方向探索先用 `选市场 200 条` 找 3-5 个候选方向；指定方向深挖再走关键词、Top100、ABA、评论 VOC
  - 已完成：导入盘点新增每个 Sheet 的有效行列识别和空格式列污染提醒，避免把空格式区域当成有效数据
  - 已完成：候选池新增搜索结果口径，区分展示行、唯一 ASIN、重复 ASIN、多关键词合并和来源文件
  - 已完成：候选池新增“候选边界二次校准”，在评论 VOC 前明确推荐主线、保留参考、优先排除和待确认问题
  - 已完成：预审 Markdown / HTML / Excel 已展示数据盘点、边界校准、建议 VOC ASIN 和数据质量口径；Excel 新增 `边界校准`、`数据质量口径` Sheet
  - 已验证：`py_compile` 通过，`git diff --check` 通过；刮窗器样例已跑通 `inspect_manual_exports -> candidate_pool -> precheck`，输出 `/tmp/amz_p18_window/precheck/precheck_report.md` 和 `precheck_data.xlsx`
  - 已验证：刮窗器卖家精灵导出 + 评论插件导出完整 workflow 已跑通，输出 `/tmp/amz_p18_window/full_workflow/final_report/report.md`
  - 已完成：P18.2 报表后多方向候选卡，基于真实卖家精灵导出把主线、旁支、混池和排除项拆成可选择方向
  - 已完成：刮窗器样例已拆出“刮条 + 海绵/布垫 + 窗户清洁组合工具”“浴室/淋浴玻璃刮水器”“汽车玻璃清洁附带场景”“贴膜/窗膜施工工具”“单独清洁液/清洁剂”等方向卡，并给出角色、状态、商品数、月销量、代表 ASIN、AI 建议和运营可选动作
  - 已完成：预审 Markdown / HTML / Excel 已展示报表后方向卡；Excel 新增 `方向候选卡` Sheet
  - 已验证：刮窗器样例已跑通 `candidate_pool -> precheck`，输出 `/tmp/amz_p18_2_window/precheck/precheck_report.md` 和 `precheck_data.xlsx`
  - 已验证：刮窗器卖家精灵导出 + 评论插件导出完整 workflow 已跑通，输出 `/tmp/amz_p18_2_window/full_workflow/final_report/report.md`
  - 已完成：P18.3 用 UI/UX Pro Max 重构预审看板和最终看板，预审看板改为“方向选择台”，最终看板改为“结论/下一步/证据链”优先的决策看板
  - 已完成：预审看板首屏新增主线建议、VOC 批次、当前动作和候选方向总览；方向卡突出主线、旁支、混池和排除项
  - 已完成：最终看板首屏收敛为当前结论、下一步、待补重点、Go/No-Go 证据和四个核心指标；竞品散点继续以“价格 × 销量分层”卡片替代，降低阅读噪音
  - 已完成：报告展示标题会自动清理 `UI重构预览`、`重新验证` 等内部调试标记，避免临时任务名污染正式报告
  - 已验证：已重跑 `/private/tmp/amz_titlefix_run2`，最终报告标题不再包含 `重新验证`
  - 进行中：按宠物牵引绳样例重新跑全流程，准备生成新的最终报告供你复核
  - 已验证：已重跑 `/private/tmp/amz_dog_leash_full_rerun`，完整接入评论 VOC、利润复核和知产/合规初筛，生成新的最终报告
  - 已完成：Excel 输出新增自动列宽，利润模板表头从 `currency/required` 调整为 `填写口径/是否必填`，打开后不再需要手动拉宽每一列
  - 已验证：`py_compile`、`git diff --check`、HTML 基本结构检查均通过；已用刮窗器真实导出生成 `/tmp/amz_uiux_precheck/precheck/precheck_dashboard.html` 和 `/tmp/amz_uiux_final/final_report/dashboard.html` 并在 Chrome 打开完成首屏目视检查
  - [已完成] VOC 主题规则从“跨品类写死”改成“按品类/方向切换”，避免窗口、牵引绳等不同品类串台；已按牵引绳样例跑通并验证规则主题显示为「免手持牵引绳」
  - [已完成] 最终报告首屏去掉重复的同段结论文案，改成“摘要 + 状态”分层展示；已确认左侧副标题改为市场事实，右侧保留状态理由
  - [已验证] 已重跑完整流程到 `/private/tmp/amz_dog_leash_rerun`，规则主题保持为「免手持牵引绳」，首屏左侧不再重复右侧状态理由，报告链路正常生成
  - [已完成] `build_review_voc_from_plugin_export.py` 修复 `_inside_chips` 末尾 `or True` bug，chip 过滤恢复语义正确
  - [已完成] 按用户反馈改造选品路线覆盖层：正式报告先拆“基础腰包款、双牵引绳+腰包升级款、多狗旁支、安全升级”等路线，再收敛供应商优先级，避免后续选品过早只盯单一款式。
  - [已验证] 宠物牵引绳样例已重建 `product_route_matrix`、HTML `产品路线对比`、Markdown `产品路线矩阵`、Excel `产品路线矩阵` Sheet；当前识别双牵引绳+腰包升级路线 3 个明确候选，斜挎/普通一拖二降为旁支；新增回归测试覆盖“双体/双套 + 腰包”路线识别。
  - [已完成] 把“多路线识别后必须逐路线小深挖”固化为后续选品流程：`research_package` 新增 `route_deep_dive_plan`，每条主线/升级/旁支路线自动生成卖家精灵、Sorftime、评价插件、1688 插件补数计划和判断门槛，再决定哪 1-2 条进入完整深挖。
  - [已验证] 宠物牵引绳样例已重建路线级小深挖：基础腰包/腰带与双牵引绳+腰包均标记为“必须路线小深挖”，双牵引绳路线当前评价粗匹配 18 条、下一步明确要求补到 30 条以上；HTML/Markdown/Excel 均展示路线级小深挖计划，新增 `路线深挖计划` Sheet；关键回归、`compileall`、`git diff --check` 和 `validate_research_outputs` 均通过。
  - [已完成] 按用户反馈继续收入口和报告表达：入口改成先问场景/痛点、不先问工厂/店铺/采购价/FBA/头程；正式 HTML/Markdown 去掉可见生成时间和保守估算展示噪音，并把 AI 综合分析改成更通俗的运营口吻。
  - [已验证] 回归覆盖入口场景/痛点、专家 AI 分析、多路线小深挖和正式报告链接；`python3 -m unittest tests.test_regression.RegressionTests.test_research_package_records_expert_ai_analysis_persona tests.test_regression.RegressionTests.test_product_route_matrix_keeps_dual_leash_waist_bag_route tests.test_regression.RegressionTests.test_interactive_workflow_initial_broad_discovery_requires_operator_boundary tests.test_regression.RegressionTests.test_interactive_workflow_targeted_deep_dive_requires_product_boundary tests.test_regression.RegressionTests.test_render_report_html_contains_formal_report_links tests.test_regression.RegressionTests.test_render_markdown_uses_formal_report_sections -v`、`python3 -m compileall -q packages scripts tests`、`git diff --check` 均通过；牵引绳运行数据已清理，待后续按新数据从零重跑。
  - [已完成] 未满足需求→验证动作：`build_research_package_from_candidate.py` 新增 `_build_validation_actions()`，把 VOC 痛点按痛点关键词映射到供应商问询项、样品测试项、Listing 风险提示、A+/视频内容建议，写入 `research_package.validation_actions`；`render_report.py` 新增 `验证动作清单` Sheet 和 Markdown 章节
  - [已完成] 重点竞品深拆卡：新增 `_build_competitor_deep_dive()`，从候选池 Top10 标杆组（取前3）和近半年新品组（取前2）生成深拆卡，每张卡包含 AI 识别特征、VOC 痛点/亮点提及、可借鉴点、硬壁垒，写入 `research_package.competitor_deep_dive`；`render_report.py` 新增 `竞品深拆卡` Sheet 和 Markdown 章节；流量词字段留空待 P19 Sorftime 补充
  - [已完成] 人工产品形态标签第一步：precheck Excel 新增 `待确认标签` Sheet，列出所有 `product_route == 待确认` 或 `tag_confidence == 低` 的 ASIN，供运营手工填写产品路线和主场景；真正的"读取修正结果并重算分布"机制留到 P17 利润/知产验收一起做
  - [已验证] `py_compile` 通过，`git diff --check` 通过；宠物牵引绳样例已跑通完整 workflow，`research_package.competitor_deep_dive` 生成 5 张卡，`validation_actions` 生成 8 条供应商问询 + 8 条样品测试 + 6 条 Listing 风险；`data.xlsx` 含 `竞品深拆卡`、`验证动作清单` Sheet；precheck Excel 含 `待确认标签` Sheet
  - 待继续：利润简版/详版拆分、Q&A / Listing 风险提示模块留到 P17 利润/知产验收一起做；运营产品路线修正结果回读机制留待后续
  - [已完成] 架构重构 / 剥离假 AI 分析层：确认「脚本 = 纯数据工具，Claude = 真正分析者」原则；从 `build_review_voc_from_plugin_export.py` 删除 `GENERIC_PAIN_RULES`/`GENERIC_HIGHLIGHT_RULES`/`VOC_RULE_PROFILES`/`build_findings()`/`build_opportunity_hypotheses()`；从 `build_research_package_from_candidate.py` 删除 `_PAIN_TO_VALIDATION`/`_build_validation_actions()`/`_FEATURE_SIGNALS`/`_learnable_points()`/`_hard_to_replicate()`；清空 `_decision_inferences()` 规则文字；从 `render_report.py` 删除 `验证动作清单` sheet/markdown 章节，更新 `竞品深拆卡` 为纯数据列；重写 `SKILL.md` 定义 Claude 作为资深选品分析师的完整框架
  - [已验证] 宠物牵引绳样例完整流程已跑通；`pain_points=[]`、`highlights=[]`、`validation_actions={}`、`decision_review.inferences=[]`、竞品深拆卡无假分析字段；`py_compile` 三文件全通过

- [x] P19 Sorftime MCP 接入与三源融合数据层
  - 状态：已完成（2026-06-12）
  - 触发条件：P18 骨架已完成；Sorftime MCP 已连通（2026-06-11）
  - 验收目标：
    1. [x] 设计三源融合工具调用策略文档（已新建 `docs/sorftime-mcp-工具调用策略.md`）
    2. [x] 无方向探索模式：`interactive_workflow.py` `_exploration_planning_card` 的 `broad_discovery` 分支补入 `search_categories_broadly` + `category_trend` + `keyword_list` 调用点（含 params_hint / credits 说明）
    3. [x] 指定方向深挖模式：`targeted_deep_dive` 分支从 `operator_export` 改为 `mcp_call` 前置快验，接入 `category_search_from_product_name` / `category_trend` / `keyword_detail`；深拆阶段接入 `product_traffic_terms` / `competitor_product_keywords` / `keyword_trend` / `similar_product_feature`
    4. [x] 候选边界校准后：`_voc_batch_planning_card` 补 `similar_product` / `potential_product` 候选扩展调用
    5. [x] candidate_pool schema 补 `sorftime_verification` 字段组（`early_exploration`、`category_trend`、`keyword_verification`、`traffic_terms`）
    6. [x] 真实样例跑通：刮窗器（window squeegee）三源融合完整链路
       - Sorftime 实测：`category_trend`（Squeegees，月销 21 万，均衡季节性）、`keyword_detail`（window squeegee 月搜 10 万，extension pole 细分词 2.7 万 CPC $1.17）、`product_traffic_terms`（B0CDBVC1W7 前 8 流量词，混池词 shower squeegee 警告）
       - 数据写入 `/tmp/amz_p19_sorftime_verification.json`，通过 `--sorftime-verification` 参数注入 `run_research_workflow.py`
       - `candidate_pool.json` 的 `sorftime_verification` 字段完整；`research_package.json` `normalized_tables.candidate.demand_evidence.sorftime_traffic_terms` 携带真实流量词
       - `render_report.py` 已修复：`_competitor_deep_dive_markdown_lines` 按 ASIN 匹配 `sorftime_traffic_terms`，命中时渲染真实词+混池警告，未命中时提示"待 Sorftime product_traffic_terms 补充"
       - 报告第七章 B0CDBVC1W7 流量词正确输出（window cleaner、squeegee 等 6 词，⚠️ 混池词：shower squeegee 3 个）
       - 21 项回归测试全部通过（包含 `targeted_deep_dive` 改 `mcp_call` 断言修复）
  - 注意：`similar_product_feature` 消耗 5 积分，只在候选方向确认后调用；其余工具每次 1 积分
  - 参考：`docs/sorftime-mcp-工具调用策略.md`

- [x] P20 对标 `zach-product-research` 的交付标准化优化
  - 状态：已完成
  - 触发条件：已对比 `https://github.com/zach22-1999/amazon-skills/tree/main/skills/zach-product-research`，确认可借鉴点为交付校验、固定章节、分析模式、Top100 属性标注/交叉分析强化、Go/No-Go 评分约束
  - 总目标：保留本项目“卖家精灵真实导出 + Sorftime 验证 + 评论插件 + 人工利润/合规复核”的路线，同时吸收对方标准化交付能力，形成可重复验收的选品工作台流程
  - 设计原则：
    - 不照搬 Sorftime 单源主流程，仍以本项目三源融合和人工复核闭环为主
    - 不把预审阶段做得过重，完整 10 章式约束只用于正式深挖报告
    - 所有强结论必须能回溯到 `import_manifest`、`candidate_pool`、`review_voc_package`、`research_package` 或人工回填模板
    - 脚本只做结构化、计算和校验；洞察、Go/Wait/No-Go、产品策略仍由 Claude 在 Skill 约束下完成

  - [x] P20.0 优化清单落盘
    - 状态：已完成
    - 验收：本计划文件新增 P20 子项，明确优化批次、验收口径和后续恢复顺序
    - 验收依据：已新增 P20.1-P20.8，覆盖交付校验器、报告章节、分析模式、Top100/交叉分析、竞品/VOC 证据、Go/Wait/No-Go、Skill 同步、多样例回归
    - 下一步：进入 P20.1 交付校验器

  - [x] P20.1 新增正式交付校验器
    - 状态：已完成
    - 目标：新增 `scripts/validate_research_outputs.py` 或等价入口，对正式输出做可机器检查的交付验收
    - 校验范围：
      - `final_report/report.md`、`report.html`、`summary.md`、`dashboard.html`、`data.xlsx` 必须存在
      - `workflow_summary.md` / `workflow_summary.json` 必须存在
      - `data.xlsx` 必须包含关键 Sheet：`数据来源说明`、`市场结构`、`Top100原始明细`、`数据质量检查`、`属性分布`、`属性交叉分析`、`竞品池`、`决策检查`、`风险矩阵`、`状态卡`
      - 评论接入时必须包含 `评论VOC`、`VOC证据`
      - 利润回填时必须包含 `利润参考结果`、`利润成本拆分`，未回填时必须在状态中明确“待填写模板”
      - 知产/合规回填时必须包含 `知产合规复核`、`知产初筛`、`合规认证预判`，未回填时必须明确“待填写模板”
      - Top100/商品明细数量不足时必须输出警告，不允许静默通过
      - 报告中必须有状态卡、下一步、待补项和数据来源说明
      - `report.html` 必须生成完整网页版正式报告，并包含 12 章核心锚点
    - 验收依据：已新增 `scripts/validate_research_outputs.py`，覆盖正式输出文件、网页版正式报告、`workflow_summary`、关键 Sheet、评论 VOC、利润/合规待填写模板、Top100 行数和报告锚点校验
    - 验收依据：新增 unittest 覆盖最小通过和缺文件失败；`python3 -m compileall -q packages scripts tests`、`python3 -m unittest discover -v`、`git diff --check` 均通过
    - 验收依据：宠物牵引绳样例 `/tmp/amz_p20_1_dog` 与刮窗器样例 `/tmp/amz_p20_1_window` 均已跑通 `run_research_workflow.py`，并通过 `python3 scripts/validate_research_outputs.py <workflow_output_dir>`
    - 下一步：进入 P20.2 正式报告章节标准化

  - [x] P20.2 正式报告章节标准化
    - 状态：已完成
    - 目标：借鉴 `zach-product-research` 的固定章节思想，但改成适合本项目的正式深挖报告结构
    - 建议章节：
      1. Executive Summary / 当前结论
      2. 数据来源与口径
      3. 候选方向与边界
      4. 市场结构与数据质量
      5. 关键词与需求信号
      6. 产品属性分布与交叉分析
      7. 竞品池与竞品选择逻辑
      8. 评论 VOC 与真实痛点
      9. 利润复核
      10. 知产/合规/退货风险
      11. Go/Wait/No-Go 决策检查
      12. 下一步动作与证据附录
    - 验收依据：`report.md` 已固定为 12 个二级章节；`validate_research_outputs.py` 已校验章节存在且顺序正确；`data.xlsx` Sheet 追溯关系已写入报告证据附录
    - 验收依据：新增 unittest 覆盖 `render_markdown` 正式章节顺序；`python3 -m compileall -q packages scripts tests`、`python3 -m unittest discover -v`、`git diff --check` 均通过
    - 验收依据：宠物牵引绳样例 `/tmp/amz_p20_2_dog` 与刮窗器样例 `/tmp/amz_p20_2_window` 均已跑通 workflow，并通过新版 `validate_research_outputs.py`
    - 下一步：进入 P20.3 分析模式库

  - [x] P20.3 建立本项目分析模式库
    - 状态：已完成
    - 目标：新增 `docs/分析模式库.md` 或 Skill reference，把报告洞察统一为可复用模式，避免纯数据罗列
    - 初始模式：
      - 数据 -> 空白 -> 机会
      - 痛点 -> 能力/供应链 -> 产品方案
      - 交叉维度 -> 结构性空白
      - 多维评分 -> 优先级矩阵
      - 待补项 -> 验证动作
    - 验收依据：已新增 `docs/分析模式库.md`，覆盖 6 种模式：数据到机会、痛点到产品方案、交叉维度到结构空白、多维评分、待补到验证动作、竞品角色到 VOC 证据链
    - 验收依据：`skills/market-scan/SKILL.md`、`skills/candidate-deep-dive/SKILL.md`、`skills/review-voc-analysis/SKILL.md` 已引用模式库，并约束至少使用对应模式
    - 验收依据：`rg "分析模式库" docs skills`、`python3 -m compileall -q packages scripts tests`、`git diff --check` 均通过
    - 下一步：进入 P20.4 Top100 属性标注与交叉分析强化

  - [x] P20.4 强化 Top100 属性标注与交叉分析标准
    - 状态：已完成
    - 目标：把 P15 基础版升级成正式深挖约束，避免属性分析只是“有输出”而不是“有决策价值”
    - 要求：
      - 每个正式深挖候选至少输出 3 个核心产品维度
      - 至少输出 3 组交叉分析，优先覆盖价格 × 销量、上架时间 × 评论门槛、产品路线 × 销量/价格
      - 低置信度/待确认标签必须进入 Excel 待确认区
      - 空白/薄供给/高需求低供给必须区分“真机会 / 伪机会 / 待验证”
    - 验收依据：`market_structure` 已新增 `pending_label_items` 和 `opportunity_judgments`；交叉分析 cell 已标注 `真机会 / 伪机会 / 待验证`
    - 验收依据：最终 Excel 新增 `待确认标签`、`机会判断` Sheet；`validate_research_outputs.py` 已校验属性定义不少于 3 个、交叉分析不少于 3 组、机会判断不为空
    - 验收依据：宠物牵引绳样例 `/tmp/amz_p20_4_dog` 与刮窗器样例 `/tmp/amz_p20_4_window` 均通过新版校验器；两组均输出 7 个属性定义、5 组交叉分析、20 条机会判断
    - 验收依据：`python3 -m compileall -q packages scripts tests`、`python3 -m unittest discover -v`、`git diff --check` 均通过
    - 下一步：进入 P20.5 竞品选择逻辑与 VOC 证据链强化

  - [x] P20.5 强化竞品选择逻辑与 VOC 证据链
    - 状态：已完成
    - 目标：正式深挖必须明确为什么选这些 ASIN 做评论/VOC 和竞品对比
    - 要求：
      - 竞品选择逻辑表覆盖量级标杆、近半年新品、功能差异、价格带、痛点参考
      - 每个竞品行至少包含 ASIN、品牌、价格、月销量、评分/评分数、选择理由、覆盖维度
      - 评论 VOC 痛点必须可追溯到 review_id、ASIN、评分和原文片段
    - 验收依据：`candidate_pool.next_review_voc_asins` 已包含竞品类型、覆盖维度和选择理由；`research_package` 新增 `competitor_selection_logic`
    - 验收依据：最终 Excel 新增 `竞品选择逻辑` Sheet；`validate_research_outputs.py` 已校验竞品选择逻辑字段和 VOC 证据链完整性
    - 验收依据：当 Claude 尚未归纳 pain_points/highlights 时，`VOC证据` 会从 `review_voc_package.normalized_reviews` 抽取原始评论兜底，保留 review_id、ASIN、评分、片段和链接
    - 验收依据：宠物牵引绳样例 `/tmp/amz_p20_5_dog` 与刮窗器样例 `/tmp/amz_p20_5_window` 均通过新版校验器；两组均输出 16 行竞品选择逻辑和 80 行完整 VOC 追溯证据
    - 验收依据：`python3 -m compileall -q packages scripts tests`、`python3 -m unittest discover -v`、`git diff --check` 均通过
    - 下一步：进入 P20.6 Go/Wait/No-Go 评分卡与状态卡标准化

  - [x] P20.6 Go/Wait/No-Go 评分卡与状态卡标准化
    - 状态：已完成
    - 目标：在不伪造结论的前提下，把当前状态卡升级为固定评分/判断框架
    - 评分维度建议：
      - 市场规模
      - 竞争格局
      - 需求清晰度
      - 新品友好度
      - 利润可行性
      - 知产/合规/退货风险
      - 数据完整度
    - 规则：利润或合规未回填时只能输出 Wait/观察/待补，禁止给强 Go
    - 验收依据：`decision_review.go_nogo_scorecard` 已固定为 7 维：市场规模、竞争格局、需求清晰度、新品友好度、利润可行性、知产/合规/退货风险、数据完整度
    - 验收依据：利润或知产/合规未回填时，评分卡写入 `gating_reasons` 并限制 `decision=WAIT`；`validate_research_outputs.py` 已校验七维齐全和未回填禁止 GO
    - 验收依据：宠物牵引绳样例 `/tmp/amz_p20_6_dog` 与刮窗器样例 `/tmp/amz_p20_6_window` 均通过新版校验器；抽查两组 `Go_No-Go评分卡` 均为 WAIT，并写明利润/合规未回填限制
    - 验收依据：`python3 -m compileall -q packages scripts tests`、`python3 -m unittest discover -v`、`git diff --check` 均通过
    - 下一步：进入 P20.7 Skill 工作流同步升级

  - [x] P20.7 Skill 工作流同步升级
    - 状态：已完成
    - 目标：把 P20 的校验器、章节结构、分析模式、评分卡写回 `skills/market-scan`、`skills/candidate-deep-dive`、`skills/review-voc-analysis`
    - 验收依据：`skills/market-scan/SKILL.md` 已同步竞品选择逻辑表、低置信标签和 VOC ASIN 覆盖要求
    - 验收依据：`skills/candidate-deep-dive/SKILL.md` 已同步 `validate_research_outputs.py`、正式 12 章报告、七维 Go/Wait/No-Go 评分卡和未回填禁止 GO 规则
    - 验收依据：`skills/review-voc-analysis/SKILL.md` 已同步 VOC 证据链回填要求和原始评论兜底口径
    - 验收依据：`rg "validate_research_outputs|12 章|七维|gating_reasons|竞品选择逻辑|VOC证据|分析模式库" skills docs/plans/V1选品系统实施计划.md`、`python3 -m compileall -q packages scripts tests`、`git diff --check` 均通过
    - 下一步：进入 P20.8 多样例回归与默认模板固化

  - [x] P20.8 多样例回归与默认模板固化
    - 状态：已完成
    - 目标：用至少两个真实样例验证 P20 全链路不是只适配单一品类
    - 必测样例：
      - 宠物牵引绳
      - 刮窗器 / 窗户清洁组合工具
    - 验收依据：README 已新增正式交付校验命令和 P20 两组真实样例回归命令
    - 验收依据：`python3 -m compileall -q packages scripts tests`、`python3 -m unittest discover -v`、`git diff --check` 均通过
    - 验收依据：宠物牵引绳样例已跑通 `/tmp/amz_p20_regression_dog`，并通过 `python3 scripts/validate_research_outputs.py /tmp/amz_p20_regression_dog`
    - 验收依据：刮窗器样例已跑通 `/tmp/amz_p20_regression_window`，并通过 `python3 scripts/validate_research_outputs.py /tmp/amz_p20_regression_window`
    - 下一步：进入下一轮真实业务试跑；若继续优化，优先补 Sorftime 深度验证样例和利润/合规回填后的完整回归

- [x] P21 架构收敛与产品化底座优化
  - 状态：已完成
  - 触发条件：用户要求“完整优化”当前项目架构；目标是在不改变 V1 业务流程的前提下，把脚本工作台收敛成可复用的应用编排底座
  - 总目标：让主流程 CLI 只保留入口壳，完整编排沉到 `packages/research_core/workflows/`；既有单功能数据工具暂保留在 `scripts/` 并由 Workflow 复用；新增数据契约校验，后续网页工作台/API/第三方数据源能复用同一套流程
  - 范围：
    - 抽出 Workflow 编排层，复用当前 `run_research_workflow.py` 的完整链路
    - 新增核心数据包契约校验，覆盖 `import_manifest`、`candidate_pool`、`research_package` 的关键结构
    - CLI 瘦身，只负责参数解析、路径展开、调用 workflow、打印结果
    - 同步 README / 架构文档 / 测试，保证后续恢复任务能看懂新边界
  - 验收依据：Workflow 应用层、Contracts 契约层、薄 CLI、测试和文档均已落地；两组真实样例通过正式交付校验

  - [x] P21.1 抽出 Workflow 应用编排层
    - 状态：已完成
    - 验收：新增 `packages/research_core/workflows/`，一键流程可通过包内函数执行；`scripts/run_research_workflow.py` 只保留 CLI 参数解析和结果打印
    - 验收依据：已新增 `packages/research_core/workflows/product_research_workflow.py`，包含 `WorkflowConfig`、`WorkflowResult`、`run_research_workflow()`、workflow summary 和 VOC 输出逻辑
    - 验收依据：`scripts/run_research_workflow.py` 已收敛为薄 CLI，参数保持兼容，只负责调用 Workflow API 并打印输出路径
    - 验收依据：`python3 -m compileall -q packages scripts tests` 通过

  - [x] P21.2 新增数据契约校验层
    - 状态：已完成
    - 验收：新增 `packages/research_core/contracts/` 或等价模块，能校验关键 JSON 数据包；workflow 在关键节点写文件前后做结构检查
    - 验收依据：已新增 `packages/research_core/contracts/validators.py`，覆盖 `import_manifest`、`candidate_pool`、`review_voc_package`、`research_package` 的关键结构校验
    - 验收依据：`run_research_workflow()` 已在 manifest、candidate_pool、VOC、research_package 关键节点调用契约校验
    - 验收依据：新增单测覆盖缺字段失败和最小有效数据包通过；`python3 -m unittest discover -v` 通过

  - [x] P21.3 补测试和回归保障
    - 状态：已完成
    - 验收：单测覆盖 workflow API、CLI 兼容、契约校验失败场景；现有真实样例继续通过正式交付校验器
    - 已完成：已补 Workflow API 真实样例单测、CLI 兼容单测、契约校验失败/通过场景单测
    - 验收依据：`python3 -m unittest discover -v` 通过，当前 13 个测试全部 OK
    - 验收依据：宠物牵引绳样例已跑通 `/tmp/amz_p21_regression_dog`，并通过 `python3 scripts/validate_research_outputs.py /tmp/amz_p21_regression_dog`
    - 验收依据：刮窗器样例已跑通 `/tmp/amz_p21_regression_window`，并通过 `python3 scripts/validate_research_outputs.py /tmp/amz_p21_regression_window`

  - [x] P21.4 同步 README 与架构文档
    - 状态：已完成
    - 验收：README 和架构文档说明新分层：CLI / Workflow / Core Rules / Renderer / Contracts；明确后续接第三方数据源的落点
    - 验收依据：README 已新增当前架构表，说明 CLI / Workflow / Contracts / Core / Renderer / Skills 分层
    - 验收依据：`packages/research_core/README.md` 已补 `workflows/`、`contracts/`、`adapters/` 职责和后续扩展落点
    - 验收依据：`docs/架构原则.md` 已从三层更新为五层技术架构，并保留“脚本做计算，Claude 做解读”的边界

  - [x] P21.5 全量验证与计划回写
    - 状态：已完成
    - 验收：`python3 -m compileall -q packages scripts tests`、`python3 -m unittest discover -v`、两组 P20 回归样例和 `validate_research_outputs.py`、`git diff --check` 均通过；本计划文件逐项回写验收依据
    - 验收依据：`python3 -m compileall -q packages scripts tests` 通过
    - 验收依据：`python3 -m unittest discover -v` 通过，13 个测试全部 OK
    - 验收依据：`python3 scripts/validate_research_outputs.py /tmp/amz_p21_regression_dog` 和 `/tmp/amz_p21_regression_window` 均通过
    - 验收依据：`git diff --check` 通过

- [x] P22 AI 交互式选品流程引擎
  - 状态：已完成
  - 触发条件：确认产品主体验不是“一键跑到底”，而是 AI 和运营实时协作；最终仍生成正式报告，但报告要沉淀交互过程中的证据、决策、暂停点和待补项
  - 总目标：新增 `workflow_state`、`decision_log`、`next_action_card`，让 AI 在两种模式下都能判断当前阶段、下一步该调 MCP / 让运营导出数据 / 给 ASIN 爬评论 / 暂停等运营决策
  - 定位：
    - `interactive_workflow`：主产品体验，AI 带运营逐步走
    - `batch_workflow`：P21 的一键流程，保留为回归测试、数据齐全后重跑报告、开发验证工具
    - `final_report`：最终交付物，读取 `research_package + workflow_state + decision_log + evidence_refs`
  - 验收依据：已完成交互式流程设计文档、`workflow_state` 契约、状态推进器、最小 CLI、测试和文档索引；最终报告仍作为交互过程沉淀物保留

  - [x] P22.1 记录交互式流程设计文档
    - 状态：已完成
    - 验收：新增 `docs/AI交互式选品流程引擎.md`，明确两种模式、阶段状态机、MCP/卖家精灵/VOC 触发规则、运营决策点、最终报告如何沉淀过程
    - 验收依据：已新增 `docs/AI交互式选品流程引擎.md`，覆盖两种模式、核心数据、阶段状态机、MCP/卖家精灵/VOC 触发规则、运营决策点、下一步动作卡和最终报告沉淀方式

  - [x] P22.2 定义 `workflow_state` / `decision_log` 数据契约
    - 状态：已完成
    - 验收：新增 schema 或 dataclass，能表达当前模式、阶段、已有输入、缺失输入、是否需要运营决策、下一步动作、证据引用和关键决策记录
    - 验收依据：已新增 `packages/research_core/workflows/interactive_workflow.py`，包含 `WorkflowState`、`DecisionRecord`、`NextActionCard`、`EvidenceRef` 等 dataclass
    - 验收依据：已新增 `packages/research_core/schemas/workflow_state.schema.json`
    - 验收依据：`python3 -m compileall -q packages scripts tests` 通过

  - [x] P22.3 实现交互式状态推进器
    - 状态：已完成
    - 验收：新增包内函数，根据当前 `workflow_state` 和可用数据生成下一步动作卡；支持无方向探索和指定方向深挖两个模式
    - 验收依据：`interactive_workflow.py` 已实现 `create_initial_state()`、`plan_next_action()`、`advance_stage()` 和 11 个阶段的下一步动作卡
    - 验收依据：单测覆盖无方向探索、指定方向深挖、阶段推进和决策日志

  - [x] P22.4 补 CLI / 测试 / 文档索引
    - 状态：已完成
    - 验收：新增最小 CLI 或测试入口生成下一步动作卡；README / 架构原则 / Skill 文档索引同步新主线
    - 验收依据：已新增 `scripts/plan_interactive_workflow.py`，可生成 `workflow_state.json` 和下一步动作卡摘要
    - 验收依据：README 已新增交互式流程状态命令；`docs/架构原则.md` 和 `packages/research_core/README.md` 已同步 interactive/batch 两条 Workflow 定位
    - 验收依据：单测覆盖 CLI 生成状态文件

  - [x] P22.5 验证与计划回写
    - 状态：已完成
    - 验收：编译、单测、`git diff --check` 通过；P22 各子项逐项回写验收依据
    - 验收依据：`python3 -m compileall -q packages scripts tests` 通过
    - 验收依据：`python3 -m unittest discover -v` 通过，17 个测试全部 OK
    - 验收依据：`python3 -m json.tool packages/research_core/schemas/workflow_state.schema.json` 通过
    - 验收依据：`python3 scripts/plan_interactive_workflow.py /tmp/amz_interactive_state.json --mode broad_discovery --intent '美国站家居清洁小工具' --site US --workflow-id smoke-interactive` 通过
    - 验收依据：`git diff --check` 通过

- [x] P23 最终报告沉淀交互式决策过程
  - 状态：已完成
  - 触发条件：P22 已完成交互式流程状态机；用户确认最终报告仍要生成，但必须沉淀 AI 与运营一起走过的暂停点、决策点和证据链
  - 总目标：让最终 `research_package` / `report.md` / `data.xlsx` 读取 `workflow_state`、`decision_log`、`next_action_card` 和 `evidence_refs`，避免报告只像一次性批处理结果
  - [x] P23.1 定义报告侧交互过程契约
    - 状态：已完成
    - 验收：Workflow 可读取可选 `workflow_state.json`，并校验关键字段后写入 `research_package.workflow_trace`
    - 验收依据：已新增 `validate_workflow_state()`；`run_research_workflow()` 可读取可选交互状态并写入 `research_package.workflow_trace`
  - [x] P23.2 报告渲染交互式决策过程
    - 状态：已完成
    - 验收：`report.md` 在 12 章结构内展示交互流程状态、下一步动作和关键决策记录；`data.xlsx` 新增 `交互决策记录` Sheet
    - 验收依据：已在 `report.md` 的 `数据来源与口径` 和 `下一步动作与证据附录` 章节写入交互流程状态、下一步动作和关键决策记录；`data.xlsx` 已新增 `交互决策记录` Sheet
  - [x] P23.3 CLI / Workflow 入口接入
    - 状态：已完成
    - 验收：`scripts/run_research_workflow.py` 支持可选 `--workflow-state`，用于把交互式流程状态合并进最终报告
    - 验收依据：已新增 `--workflow-state` 参数，`WorkflowConfig` 和批量流程可直接消费交互状态文件
  - [x] P23.4 验证与文档回写
    - 状态：已完成
    - 验收：补单测、编译、回归和文档说明；验证通过后回写本计划
    - 验收依据：`python3 -m compileall -q packages scripts tests`、`python3 -m unittest discover -v`、`python3 scripts/validate_research_outputs.py /private/tmp/amz_p23_smoke`、`git diff --check` 均通过；README 与交互式流程文档已同步

- [x] P24 对标项目补数据管道与清理旧包袱
  - 状态：已完成
  - 触发条件：用户确认要把对标分析里的 Task2/Task3 直接落地到当前仓库
  - 总目标：把 `category_report` / `ali1688_similar_product` 的信号真正接进数据管道，同时清理被新架构替代的旧文档和冗余脚本
  - [x] P24.1 接入 Sorftime 新信号
    - 状态：已完成
    - 验收：`sorftime_verification` 能承载 `category_report` 和 `ali1688_similar_product` 的标准化结果；`candidate_pool` / `research_package` / 报告能消费这些信号
    - 验收依据：已新增 `category_report_snapshot` 与 `supply_chain_signal` 解析；报告市场章节展示 Sorftime Top 快照，利润章节和 `供应链粗估` Sheet 展示 1688 中国站人民币粗采购价；新增单测覆盖 `sorftime_verification -> candidate_pool -> research_package -> report`
  - [x] P24.2 清理过期文档和冗余脚本
    - 状态：已完成
    - 验收：确认可删除或归档的旧文档、旧计划和重复脚本，并回写台账
    - 验收依据：已删除旧计划草稿、早期自查工具三件套、静态报告 Mock、`build_zero_to_one_plan.py` 和旧 `seller-sprite-product-research` Skill 目录；README 文档索引已收敛到当前有效文档与新 Master Skill
  - [x] P24.3 验证与回写
    - 状态：已完成
    - 验收：补单测、回归和文档索引；完成后回写本计划
    - 验收依据：`python3 -m compileall -q packages scripts tests`、`python3 -m json.tool packages/research_core/schemas/candidate_pool.schema.json`、`python3 -m unittest discover -v`、`python3 scripts/plan_interactive_workflow.py /tmp/amz_p24_workflow_state.json --mode targeted_deep_dive --intent '窗户刮水器二合一工具' --site US --workflow-id p24-smoke`、`python3 scripts/run_research_workflow.py 卖家精灵导出样例_美国站_宠物牵引绳_20260607 /tmp/amz_p24_regression --site US --task-name P24回归 --workflow-state /private/tmp/amz_p24_workflow_state.json`、`python3 scripts/validate_research_outputs.py /private/tmp/amz_p24_regression`、`git diff --check` 均通过

- [ ] P25 Codex 客户端真实选品闭环试跑
  - 状态：进行中；本轮从 Stage 0 意图收集开始，目标是不用 Web 外壳，直接验证 Codex + Skills + MCP + 本地脚本是否能按真实运营节奏跑通
  - 触发条件：用户确认“现在可以开始跑一遍”
  - 验收：从真实选品意图出发，完成 AI 判断模式、Sorftime 快探、卖家精灵导出引导、数据盘点、候选池、VOC ASIN 批次、评论 VOC、最终报告和校验；中途每个运营决策点都记录到台账
  - 进行中：已确认使用 `skills/amazon-product-research/SKILL.md` 作为本轮主流程，先按 Stage 0 每次只问一个问题收集意图
  - 已确认：本项目默认目标站点固定为美国站 `US`，本轮不再重复询问站点
  - 已确认：本轮方向为“宠物牵引绳 / dog leash”，自动进入指定方向深挖；禁区按默认保守规则处理：儿童用品、食品接触、医疗健康、强认证、带电、大件、明显侵权风险先排除
  - 已重置：2026-06-16 按用户要求删除宠物牵引绳相关源数据与生成物，旧 `/tmp/amz_p25_pet_leash` 报告不得再引用
  - 下一步：等待用户确认是否按“免手持弹力腰带牵引绳”继续，并按新导出清单提供卖家精灵数据
  - [x] Stage 0 意图收集与初始候选假设卡
    - 验收依据：已确认默认美国站 `US`、方向“宠物牵引绳 / dog leash”、默认保守禁区；模式判定为指定方向深挖
  - [x] Stage 1 Sorftime 快探 / 方向快验
    - 验收依据：已重新生成 `runs/20260616_pet_leash/stage1_pet_leash_quick_research.md` 和 `workflow_state.json`
    - 验收依据：Sorftime 新快验覆盖 `dog leash`、`hands free dog leash`、`bungee dog leash`、`dog running leash hands free` 等关键词；结论为泛词混池严重，建议主线收窄到免手持弹力腰带牵引绳
    - 验收依据：1688 中国站人民币快验已重新执行，目标形态粗采购价约 RMB 12-30/件，带腰包/高配款约 RMB 20-38/件；仅作为 Stage 1 粗估，不替代后续利润模板
    - 已验证：Sorftime HTTP MCP 通过 quick URL 方式可列出 81 个工具，包含 `keyword_detail`、`category_report`、`ali1688_similar_product`；当前 Codex 动态工具桥如仍报 `unsupported call`，需要重启/新会话加载 `~/.codex/config.toml`
  - [x] Stage 2 卖家精灵精准导出清单
    - 待用户提供：按新快验报告导出 `Hands Free Leashes` 市场分析、`hands free dog leash` / `bungee dog leash` / `dog running leash hands free` 搜索结果、核心 ASIN 关键词反查和 ABA 关键词趋势
    - 进行中：已收到 `runs/20260616_pet_leash/inputs/seller_sprite` 下 4 个 Excel；`KeywordResearch-*` 为关键词研究/扩展表，`Market-researchHandsFreeLeashes-US-Last-30-days.xlsx` 为选市场单行列表，暂不能替代 Top100 商品搜索结果、完整市场分析详情和 ASIN 关键词反查
    - 下一步：优先补导 `hands free dog leash`、`bungee dog leash` 的搜索结果/产品调研 Top100 明细；如卖家精灵支持，再进入 `Hands Free Leashes` 市场分析详情页导出完整市场分析
    - 已收到：新增 `Competitor-US-Last-30-days-433222.xlsx`、`Competitor-US-Last-30-days-433246.xlsx`，已识别为搜索结果/产品调研明细；两表合计 3807 行、3309 个唯一 ASIN、重复 ASIN 498 个
    - 待补数据：仍缺完整市场分析详情和核心 ASIN 关键词反查；`KeywordResearch-*` 可作为关键词需求参考，但不能替代 ASIN 关键词反查
    - 已完成：已收到完整市场分析 `US-Market-Hands_Free_Leashes-Last-30-days-435882.xlsx` 和 5 个 `_ReverseASIN-*` 关键词反查文件，三类核心数据源齐备
    - 验收依据：`import_manifest.json` 已识别 `seller_sprite_market_analysis`、`seller_sprite_search_results`、`seller_sprite_reverse_asin_keywords`，缺失项清零
  - [x] Stage 3 导出数据盘点与缺失检查
    - 已重置：旧 `import_manifest` 与 `/tmp/amz_p25_pet_leash` 生成物已删除，待重新盘点
    - 待补数据：已生成 `runs/20260616_pet_leash/import_manifest.json`；当前 4 个 Excel 均只能识别为 `unknown`，缺少 `seller_sprite_search_results`、`seller_sprite_market_analysis`、`seller_sprite_reverse_asin_keywords`
    - 已盘点：重新生成 `runs/20260616_pet_leash/import_manifest.json`；当前已具备 `seller_sprite_search_results`，仍缺 `seller_sprite_market_analysis`、`seller_sprite_reverse_asin_keywords`
    - 已完成：重新盘点后缺失项为 0；可进入候选池与预审重建
  - [ ] Stage 4 双源合并分析与候选池
    - 已重置：旧 `candidate_pool` 与预审报告已删除，待重新生成
    - 进行中：已基于现有搜索结果生成草稿 `runs/20260616_pet_leash/candidate_pool.json` 和 `runs/20260616_pet_leash/precheck/*`；仅用于先看商品池和 VOC ASIN 批次，正式结论需补市场详情、关键词反查、评论 VOC、利润和合规复核
    - 已生成：基于完整市场分析、搜索结果和 ASIN 关键词反查重建 `candidate_pool.json` 与 `precheck/*`；结论为“继续看”，但需先做候选边界校准和评论 VOC
  - [ ] Stage 5 候选边界校准与 VOC ASIN 批次
    - 已重置：旧 VOC ASIN 批次来自已删除预审报告，待重新生成后再确认
  - [x] Stage 6 评论 VOC 分析
    - 已重置：旧宠物牵引绳评论插件导出文件已删除，等待用户重新验证/重新提供评论数据
    - 进行中：已收到评论插件 JSON `runs/20260616_pet_leash/inputs/seller_sprite/B07R56CBWX-multi-2026-06-16.json`，包含 399 条评论、5 个 ASIN、采集入口 CA；准备生成 `review_voc_package` 并做实质 VOC 分析
    - 已完成：已生成 `runs/20260616_pet_leash/review_voc/review_voc_package.json`、`voc_report.md`、`voc_summary.md`、`voc_evidence.xlsx` 和人工分析 `voc_analysis.md`
    - 验收依据：实际评论明细覆盖 399 条、4 个 ASIN、低分 77 条、含图/视频 32 条；核心痛点为腰带锁紧、尺寸适配、扣具/弹力绳/缝线耐用、控狗安全和长度/手柄位置；结论为 Wait / 谨慎继续
  - [ ] Stage 7 深挖报告生成与交付校验
    - 已重置：旧最终报告、看板、Excel 与校验输出已删除，待重新生成
    - 进行中：已接入 `review_voc_package` 生成 `research_package.json` 和 `final_report/*`，待用户先看 VOC 结论后再继续利润/合规复核
    - 进行中：按运营反馈把报告生成前提固化为“资深亚马逊运营专家”视角，准备让 `research_package` 与 HTML 报告共同呈现四源数据融合后的专家判断口径
    - 已完成：正式 HTML 报告已调整为更克制的运营工作台风格；1688 优先联系清单改为单列宽卡，图片固定左侧、价格/起订量/匹配度/待确认项右侧展示，避免大图撑乱版面
    - 验收依据：已重新生成 `runs/20260616_pet_leash/final_report/report.html`；桌面/移动截图输出到 `output/playwright/pet-leash-report-desktop-final.png`、`output/playwright/pet-leash-report-mobile-final.png`；`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 通过，剩余警告为既有竞品角色覆盖不足
    - 已完成：用户反馈供应链清单标题位置偏上、整体深绿色太重；已改为更清新的浅蓝绿 UI，去掉首屏深绿色大块和顶边，优先联系清单增加独立标题区、分隔线和说明文案
    - 验收依据：已重新生成 `runs/20260616_pet_leash/final_report/report.html`；新版截图输出到 `output/playwright/pet-leash-report-fresh-hero.png`、`output/playwright/pet-leash-report-fresh-supply.png`、`output/playwright/pet-leash-report-fresh-mobile.png`；`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 通过，剩余警告仍为既有竞品角色覆盖不足
    - 已完成：用户反馈供应链商品图片左上角文案会与序号角标重叠；已把序号从图片内角标改成独立编号列，避免遮挡供应商图片自带文案
    - 验收依据：已重新生成 `runs/20260616_pet_leash/final_report/report.html`；截图输出到 `output/playwright/pet-leash-report-rankfix-card3.png`、`output/playwright/pet-leash-report-rankfix-supply.png`、`output/playwright/pet-leash-report-rankfix-mobile.png`；`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 通过
    - 已完成：用户反馈正式 HTML 报告首屏标题批次日期和“生成时间”标签无运营价值；已清理界面展示，标题去掉末尾 `_YYYYMMDD` 批次号，首屏不再显示生成时间，但底层 metadata 仍保留用于追溯
    - 验收依据：已重新生成 `runs/20260616_pet_leash/final_report/report.html`；截图输出到 `output/playwright/pet-leash-report-time-clean-hero.png`；HTML 检查确认不再包含首屏批次标题和生成时间 chip；`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 通过
    - 已完成：按用户确认重新优化为四源融合正式报告，不再只做 UI 美化；已把卖家精灵、Sorftime、评价插件、1688 插件同时写入报告首屏“数据融合口径”和市场验证链路
    - 已完成：补充 `runs/20260616_pet_leash/sorftime_verification.json`，覆盖 7 个 Sorftime `keyword_detail`、`Basic Leashes` 类目快照/趋势/类目关键词和 5 个竞品 ASIN 流量词；`candidate_pool.json` 与 `research_package.json` 已重建并包含 `sorftime:*` 数据源
    - 已完成：1688 优先联系清单改为图片左侧、编号/匹配度在右侧信息区的小标签，移除独立编号列和图片角标，避免序号与供应商图片文案重叠；供应商图优先使用本地 `visual_review_assets` 缓存，避免远程图加载失败
    - 验收依据：`runs/20260616_pet_leash/final_report/report.html` 已重建；HTML 检查确认包含 `数据融合口径`、`Sorftime 实时验证`、14 张本地供应链图，不再包含 `.supplier-rank` 和首屏 `生成 2026` 时间 chip
    - 验收依据：HTTP 预览日志确认 14 张 `runs/20260616_pet_leash/supply_chain/visual_review_assets/images/*` 供应链图片均返回 200；截图输出到 `output/playwright/pet-leash-final-desktop.png`、`output/playwright/pet-leash-final-mobile.png`、`output/playwright/pet-leash-final-supply-cards.png`
    - 验收依据：`python3 -m compileall -q packages scripts tests`、`python3 -m unittest discover -v`、`git diff --check`、`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 均通过；正式交付校验显示 `data.xlsx` 31 个 Sheet，Top100 原始明细 3319 行，评论 VOC 关键 Sheet 完整
    - 已完成：按用户反馈补 UI 细节，评分简表统一显示 `x/10` 并补充“满分 10 分，分数越高代表越有利”；“观察待核”改为和其他小节一致的标题区与说明文案，避免标题贴上沿
    - 验收依据：已重新生成 `runs/20260616_pet_leash/final_report/report.html`；HTML 检查确认包含 `8.0/10`、`9.5/10`、评分满分说明和观察待核说明；`python3 -m compileall -q packages scripts tests`、`git diff --check`、`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 均通过
    - 已完成：修复风险矩阵 `评论/VOC` 状态判断；当评价插件已提供评论汇总但尚未人工归纳 `pain_points` 时，报告按已接入评论证据展示，不再误写“未接入评论 VOC”
    - 验收依据：`runs/20260616_pet_leash/final_report/report.html` 和 `report.md` 已重建并显示已接入 399 条评论、覆盖 4 个 ASIN、低分 77 条、含图/视频 32 条；新增回归测试覆盖无 `pain_points` 但有 VOC 汇总的场景；`python3 -m compileall -q packages scripts tests`、`git diff --check`、`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash`、`python3 -m unittest discover -v` 均通过
    - 已完成：正式 HTML 报告新增 `AI 综合分析` 层，把市场、Sorftime、评价 VOC 和 1688 供应链合成运营判断，并明确“继续看但不要直接立项”、产品规格转化和供应商验证动作
    - 验收依据：`runs/20260616_pet_leash/final_report/report.html` 已重建；HTML 检查确认包含 `AI 综合分析`、`综合判断`、`VOC 要落成产品规格` 和 `供应商要验证到证据`；Chrome 截图输出到 `output/playwright/pet-leash-ai-analysis-desktop.png`、`output/playwright/pet-leash-ai-analysis-section.png`、`output/playwright/pet-leash-ai-analysis-mobile.png`，桌面/移动端无横向溢出；`python3 -m compileall -q packages scripts tests`、`git diff --check`、`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 均通过
    - 已完成：按用户确认把报告生成前提固化为“资深亚马逊运营专家”视角；`research_package` 新增 `ai_analysis.persona / data_source_scope / decision_principle / insights`，HTML 与 Markdown 正式报告均展示专家分析口径，并同步 Master Skill 与深挖 Skill
    - 验收依据：`runs/20260616_pet_leash/research_package.json` 和 `final_report/research_package.json` 均包含 `ai_analysis.persona=资深亚马逊运营专家`；`report.html` 包含 `资深亚马逊运营专家视角` 和 `数据够多是为了降低误判`；`report.md` 包含 `AI 综合分析口径`；新增回归测试 `test_research_package_records_expert_ai_analysis_persona` 通过；`python3 -m unittest tests.test_regression.RegressionTests.test_research_package_records_expert_ai_analysis_persona tests.test_regression.RegressionTests.test_render_markdown_uses_formal_report_sections tests.test_regression.RegressionTests.test_render_report_html_contains_formal_report_links -v`、`python3 -m compileall -q packages scripts tests`、`git diff --check`、`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 均通过，剩余警告仍为既有竞品角色覆盖不足
    - 已完成：用户反馈 `AI 综合分析` 卡片文案偏官方、数据堆叠太重；已改成“先说通俗结论，再用少量关键数字解释”，例如“有量，但不能拿普通款硬冲”“评论里最要命的是用起来稳不稳”“这些没补齐前，最多是继续看，不是直接开干”
    - 验收依据：`runs/20260616_pet_leash/final_report/report.html` 已重建；AI 分析区实际文案已收敛为运营口语表达，数据源说明改为“看市场大不大/看买家真实吐槽/看有没有货”等通俗说明；`python3 -m unittest tests.test_regression.RegressionTests.test_research_package_records_expert_ai_analysis_persona -v`、`python3 -m compileall -q packages scripts tests`、`git diff --check`、`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 均通过
  - [ ] Stage 8 利润 / 合规复核
    - 已确认：1688 采购价口径固定为中国站 `https://www.1688.com/` / `*.1688.com` 人民币 RMB/CNY 报价；当前通过 Sorftime MCP 间接查询，不用 Alibaba 国际站 USD 报价替代
    - 已完成：用户确认 1688 价格区间应按最高价做保守估算；已把 1688 插件导出改为展示采购价区间、利润/风险默认按区间上限保守估算，并同步 HTML / Markdown / Excel。
    - 验收依据：`runs/20260616_pet_leash/final_report/report.html` 已重建，供应商卡显示 `¥19-27` 且标注 `估算按 ¥27`；供应链汇总显示 `RMB 1-32（保守按 RMB 32）`；`python3 -m unittest tests.test_regression.RegressionTests.test_1688_plugin_export_builds_and_applies_supply_chain_signal tests.test_regression.RegressionTests.test_ali1688_raw_result_flows_to_supply_chain_report tests.test_regression.RegressionTests.test_sorftime_category_report_and_supply_chain_flow_to_report tests.test_regression.RegressionTests.test_research_package_records_expert_ai_analysis_persona tests.test_regression.RegressionTests.test_render_report_html_contains_formal_report_links -v`、`python3 -m compileall -q packages scripts tests`、`git diff --check`、`python3 scripts/validate_research_outputs.py runs/20260616_pet_leash` 均通过。
    - 已重置：旧 1688 验证快照和旧报告已删除，待重新验证后再写入采购价证据
    - 待运营确认：利润模板、FBA 费用、头程、入库配置费、目标售价、退货和广告假设、知产/合规初筛均需重新回填
    - 已完成：代码层已补 1688 域名与 RMB/CNY 硬校验；非 1688、中国站链接缺失、Alibaba 国际站 USD、币种不明样本不进入采购价区间，并记录剔除数量和原因
    - 验收依据：新增反例单测覆盖混合有效/无效样本、全无效样本；`python3 -m unittest discover -v` 通过，41 项 OK、5 项因本地样例缺失跳过；`python3 -m compileall -q packages scripts tests` 和 `git diff --check` 通过
  - [ ] Stage 9 Go / Wait / No-Go 最终判断
    - 待重新验证：宠物牵引绳旧数据已删除，重新跑完 Stage 1-8 后再判断

## 下次恢复顺序

1. 读取本文件。
2. 找到第一个未完成的 `P` 项。
3. 开始前把该项状态改成“进行中”。
4. 完成并验证后再勾选。
5. 不要跳过 `selection_brief` 和 `candidate_pool` 直接接手动导入/MCP。

## 何时需要你提供卖家精灵导出数据 / MCP

当前已经完成 P1-P14。P15 先做数据质量、属性打标和交叉分析，暂不需要 MCP。

需要数据时，我会明确告诉你：

- 需要从卖家精灵哪个功能页面导出什么表
- 要跑哪个站点
- 要用什么关键词/大概方向测试
- 需要哪些列、文件命名和筛选条件
- 后续如果切 MCP，再说明需要哪些 MCP 工具和额度
